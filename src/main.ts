// import { platformBrowserDynamic } from '@angular/platform-browser-dynamic';

// import { AppModule } from './app/app.module';


// platformBrowserDynamic().bootstrapModule(AppModule)
//   .catch(err => console.error(err));
// Decorators are functions that return functions - tell angular what the code should do and how angular should inject
// that code into other part of the application
// they are marked using "@" symbol 
// invoked at runtime with arguments
// eg. @NgModule(), @Component(), @Injectable, @Pipe() , @DIredctive(), inputoutput
// errors - missing, parentheses, properties , values and semicolon