// collections of fiels that work together
// Standalone componenets - let you use files wihtout modules
// to declare a class as a module use @NgModule decorator
// @NgModule{
//     {declarations:[
//         AppComponent
//     ],
//     imports:[

//     ]
// }
// }
// this decorator take a configuration object as the only argument
// one special property is bootstrap which tells angular which component to load
// only the root module should have the bootstrap property in module architecture

// Angular Components
// These are the main building blocks in all Angular apps
// four parts - html,  css, Tyepscript, component selector
// reusablility -- @Component decorator also takes only a configuration object as it's only argument

// Component class itself

